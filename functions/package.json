{
  "name": "cueweb2-functions",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "18"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^5.1.0"
  },
  "scripts": {
    "lint": "eslint . || true",
    "deploy": "firebase deploy --only functions"
  }
}
